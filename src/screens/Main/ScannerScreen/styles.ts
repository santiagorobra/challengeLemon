import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    marginBottom: 12,
  },
  listContainer: {
    paddingBottom: 24,
  },
  emptyContainer: {
    alignItems: 'center',
    marginTop: 24,
  },
  scanButton: {
    marginBottom: 12,
  }
});

export default styles;
